"""Game-agnostic Win16 layer over the dos_re VM: NE loading + Windows-API services.

This package never learns a specific game (no game addresses, filenames, or
formats) — it is the Win16 analogue of the dos_re framework itself and a
candidate for promotion into dos_re once proven.
"""
